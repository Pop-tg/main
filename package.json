{
  "name": "pop.tg",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build:frontend": "lerna run --scope frontend build",
    "build:api": "lerna run --scope api build"
  }
}
